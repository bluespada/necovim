return {
    -- users color scheme
    colorscheme = "default",

    -- config init
    init = function()end,

    -- plugins
    plugins = {},

    -- termguicolors
    termguicolors = false,

    -- nowrap
    nowrap = nil,

    -- keybind
    keybind = function(map, wmap) end,

    -- wiki
    wiki = {

        path = '~/note',

        extension = ".md",
    }
}
